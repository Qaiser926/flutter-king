name: expandable_listview_example
version: 1.0.0+1

environment:
  sdk: ">=2.3.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.0

  flushbar: ^1.10.4
  sqflite: ^2.0.0+4
  intl: ^0.17.0
  flutter_staggered_grid_view: ^0.4.1
  path: ^1.8.0
  flutter_toastr: ^1.0.3
  

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true